var $new_link = jQuery("<%= escape_javascript(link_to 'Leave Group', {:action => 'unjoin_group', :id => @disc_group.id }, :remote=>true, :class => "join_group_d", :id => "unjoin_group_#{@disc_group.id }").html_safe %>");
jQuery("#join_group_<%= @disc_group.id %>").replaceWith($new_link);
